/// <reference types="vite/client" />

// Provided by vite.config.ts's changelogPlugin, not a real file on disk.
declare module "virtual:changelog" {
  const changelog: string;
  export default changelog;
}
